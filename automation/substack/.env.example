# Option 1: Email + Password (simpler)
SUBSTACK_EMAIL=your@email.com
SUBSTACK_PASSWORD=your_password

# Option 2: Cookies (if no password set)
# Get cookies: Browser → finitematter.substack.com → F12 → Network → copy cookie string
# SUBSTACK_COOKIES_STRING=substack.sid=xxx; substack.lli=xxx
