---
// Portfolio Showcase — Logo grid with grayscale → color hover
---

<section class="portfolio" id="portfolio">
  <div class="portfolio-container">
    <span class="section-tag">Portfolio</span>

    <h2 class="portfolio-title fade-in">
      Gründer die bauen, was vor KI nicht möglich war.
    </h2>

    <p class="portfolio-body fade-in">
      Wir backen Pre-Seed und Seed Gründer in regulierten Industrien —
      Legal, Healthcare, Finance, Manufacturing — die KI-first Produkte bauen,
      nicht KI auf Legacy-Software draufschrauben.
    </p>

    <p class="portfolio-body fade-in">
      Unsere Checkgröße: $250K–$750K. Unser Value-Add: Architecture Review,
      Technical Diligence und operative Playbooks vom Skalieren von B2B SaaS.
      Wir waren in Ihren Schuhen. Wir kennen den Unterschied zwischen Demo
      und Deployment.
    </p>

    <!-- Portfolio Logo Grid -->
    <div class="portfolio-grid fade-in">
      <div class="portfolio-logo-placeholder">
        <div class="logo-box">
          <span class="mono">Demnächst</span>
        </div>
      </div>
      <div class="portfolio-logo-placeholder">
        <div class="logo-box">
          <span class="mono">Portfolio 2</span>
        </div>
      </div>
      <div class="portfolio-logo-placeholder">
        <div class="logo-box">
          <span class="mono">Portfolio 3</span>
        </div>
      </div>
      <div class="portfolio-logo-placeholder">
        <div class="logo-box">
          <span class="mono">Portfolio 4</span>
        </div>
      </div>
    </div>

    <div class="portfolio-cta fade-in">
      <a href="/de/portfolio" class="btn-secondary">
        Unsere Gründer kennenlernen →
      </a>
    </div>
  </div>
</section>

<style>
  .portfolio {
    padding: 8rem 2rem;
    background: var(--bg-deep);
  }

  .portfolio-container {
    max-width: var(--content-narrow);
    margin: 0 auto;
  }

  .portfolio-title {
    font-size: clamp(2rem, 3.5vw, 2.625rem);
    font-weight: 600;
    line-height: 1.2;
    letter-spacing: -0.02em;
    color: var(--white-pure);
    margin-bottom: 2rem;
  }

  .portfolio-body {
    font-size: 1.125rem;
    line-height: 1.75;
    color: var(--gray-light);
    margin-bottom: 1.5rem;
  }

  .portfolio-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
    margin: 3rem 0;
  }

  .portfolio-logo-placeholder {
    aspect-ratio: 3 / 2;
    background: var(--bg-card);
    border: 1px solid var(--gray-rule);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }

  @media (hover: hover) {
    .portfolio-logo-placeholder:hover {
      border-color: rgba(200, 170, 80, 0.2);
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
    }
  }

  .logo-box {
    text-align: center;
  }

  .logo-box .mono {
    font-size: 0.75rem;
    opacity: 0.5;
  }

  .portfolio-cta {
    text-align: center;
    margin-top: 3rem;
  }

  @media (max-width: 768px) {
    .portfolio {
      padding: 5rem 1.5rem;
    }

    .portfolio-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }
  }
</style>

<script>
  // Intersection Observer for fade-in
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    },
    { threshold: 0.1 }
  );

  document.querySelectorAll('.portfolio .fade-in').forEach(el => {
    observer.observe(el);
  });
</script>
