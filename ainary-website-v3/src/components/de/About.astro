---
// About Section — Vision/Mission, Florian+Mia Positionierung
---

<section class="about" id="about">
  <div class="about-container">
    <span class="section-tag">Über uns</span>

    <h2 class="about-title fade-in">
      Integrität wirkt stärker als Hype.
    </h2>

    <div class="about-body fade-in">
      <p>
        Ainary Ventures ist eine Multi-Arm-Plattform, gebaut von Machern für Macher.
      </p>

      <p>
        Wir investieren in Gründer, die echte Probleme in traditionellen Industrien lösen —
        Manufacturing, Legal, Enterprise Operations. Wir bauen KI-Produkte, die in
        Produktionsumgebungen laufen, nicht nur Sandbox-Demos. Und wir lehren transparent,
        was wir lernen — inklusive was nicht funktioniert hat.
      </p>

      <p>
        Jeder Arm füttert die anderen: Produkte beweisen technische Tiefe, Consulting
        bringt Deal Flow, Investments validieren unsere These, und Content vergrößert
        unsere Reichweite. Ein Schwungrad, das kein Einzelarm-Wettbewerber replizieren kann.
      </p>

      <p class="about-closing">
        Wir sind ehrlich darüber, was funktioniert und was nicht, denn Integrität
        wirkt stärker als Hype.
      </p>
    </div>

    <!-- Flywheel Visual -->
    <div class="flywheel fade-in">
      <div class="flywheel-node">
        <div class="flywheel-icon" aria-hidden="true">🔨</div>
        <h3 class="flywheel-label">Bauen</h3>
        <p class="flywheel-desc">Produkte beweisen Tiefe</p>
      </div>
      <div class="flywheel-arrow" aria-hidden="true">→</div>
      <div class="flywheel-node">
        <div class="flywheel-icon" aria-hidden="true">💰</div>
        <h3 class="flywheel-label">Investieren</h3>
        <p class="flywheel-desc">Kapital validiert These</p>
      </div>
      <div class="flywheel-arrow" aria-hidden="true">→</div>
      <div class="flywheel-node">
        <div class="flywheel-icon" aria-hidden="true">🚀</div>
        <h3 class="flywheel-label">Deployen</h3>
        <p class="flywheel-desc">Kunden bekommen Produktions-KI</p>
      </div>
      <div class="flywheel-arrow" aria-hidden="true">→</div>
      <div class="flywheel-node">
        <div class="flywheel-icon" aria-hidden="true">📚</div>
        <h3 class="flywheel-label">Lehren</h3>
        <p class="flywheel-desc">Content vergrößert Reichweite</p>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    padding: 8rem 2rem;
    background: var(--bg-deep);
  }

  .about-container {
    max-width: var(--content-narrow);
    margin: 0 auto;
  }

  .about-title {
    font-size: clamp(2rem, 3.5vw, 2.625rem);
    font-weight: 600;
    line-height: 1.2;
    letter-spacing: -0.02em;
    color: var(--white-pure);
    margin-bottom: 2.5rem;
  }

  .about-body {
    margin-bottom: 4rem;
  }

  .about-body p {
    font-size: 1.125rem;
    line-height: 1.75;
    color: var(--gray-light);
    margin-bottom: 1.5rem;
  }

  .about-closing {
    font-weight: 500;
    color: var(--white-warm);
  }

  /* Flywheel Visual */
  .flywheel {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    flex-wrap: wrap;
    padding: 3rem 0 0;
  }

  .flywheel-node {
    text-align: center;
    max-width: 160px;
  }

  .flywheel-icon {
    font-size: 2rem;
    margin-bottom: 0.75rem;
  }

  .flywheel-label {
    font-family: var(--font-display);
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--white-pure);
    margin-bottom: 0.5rem;
  }

  .flywheel-desc {
    font-size: 0.875rem;
    color: var(--gray-mid);
    line-height: 1.4;
  }

  .flywheel-arrow {
    font-size: 1.5rem;
    color: var(--gold-base);
    opacity: 0.5;
  }

  @media (max-width: 768px) {
    .about {
      padding: 5rem 1.5rem;
    }

    .flywheel {
      flex-direction: column;
      gap: 2rem;
    }

    .flywheel-arrow {
      transform: rotate(90deg);
    }

    .flywheel-node {
      max-width: 100%;
    }
  }
</style>

<script>
  // Intersection Observer for fade-in
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    },
    { threshold: 0.2 }
  );

  document.querySelectorAll('.about .fade-in').forEach(el => {
    observer.observe(el);
  });
</script>
