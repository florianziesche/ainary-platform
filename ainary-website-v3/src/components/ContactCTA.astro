---
// Contact CTA Section — Contact form/CTA with direct contact info
---

<section class="contact" id="contact">
  <div class="contact-container">
    <span class="section-tag">Contact</span>

    <h2 class="contact-title fade-in">
      Let's Build Something That Ships.
    </h2>

    <p class="contact-body fade-in">
      Whether you're a founder raising your first round, an enterprise
      deploying AI to production, or an LP evaluating emerging managers —
      we'd rather have a real conversation than a sales pitch.
    </p>

    <p class="contact-body fade-in">
      No forms. No autoresponders. Just a direct line.
    </p>

    <!-- Contact Options -->
    <div class="contact-options fade-in">
      <div class="contact-option">
        <h3 class="contact-option-title">For Everyone</h3>
        <a href="mailto:hello@ainary.vc" class="contact-link">
          hello@ainary.vc
        </a>
      </div>

      <div class="contact-option">
        <h3 class="contact-option-title">For Founders</h3>
        <a href="mailto:founders@ainary.vc" class="contact-link">
          founders@ainary.vc
        </a>
        <p class="contact-option-note">
          Send us your deck. We respond to every one within 5 business days.
        </p>
      </div>

      <div class="contact-option">
        <h3 class="contact-option-title">For Consulting</h3>
        <a href="mailto:consulting@ainary.vc" class="contact-link">
          consulting@ainary.vc
        </a>
        <p class="contact-option-note">
          Tell us the problem. We'll tell you if we can help.
        </p>
      </div>
    </div>

    <!-- Social Links -->
    <div class="contact-social fade-in">
      <a href="https://linkedin.com/in/florian-ziesche" target="_blank" rel="noopener noreferrer" class="contact-social-link">
        LinkedIn
      </a>
      <span class="contact-social-divider">·</span>
      <a href="https://x.com/Florian1776" target="_blank" rel="noopener noreferrer" class="contact-social-link">
        X (Twitter)
      </a>
      <span class="contact-social-divider">·</span>
      <a href="https://florianziesche.substack.com" target="_blank" rel="noopener noreferrer" class="contact-social-link">
        Substack
      </a>
    </div>

    <!-- CTA Button -->
    <div class="contact-cta fade-in">
      <a href="mailto:hello@ainary.vc" class="btn-cta btn-cta-solid">
        Send a message →
      </a>
    </div>
  </div>
</section>

<style>
  .contact {
    padding: 8rem 2rem;
    background: var(--bg-base);
  }

  .contact-container {
    max-width: var(--content-narrow);
    margin: 0 auto;
    text-align: center;
  }

  .contact-title {
    font-size: clamp(2rem, 3.5vw, 2.625rem);
    font-weight: 600;
    line-height: 1.2;
    letter-spacing: -0.02em;
    color: var(--white-pure);
    margin-bottom: 2rem;
  }

  .contact-body {
    font-size: 1.125rem;
    line-height: 1.75;
    color: var(--gray-light);
    margin-bottom: 1.5rem;
  }

  .contact-options {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin: 4rem 0 3rem;
  }

  .contact-option {
    text-align: center;
  }

  .contact-option-title {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--gold-base);
    margin-bottom: 0.75rem;
  }

  .contact-link {
    display: block;
    font-size: 1.125rem;
    font-weight: 500;
    color: var(--white-pure);
    text-decoration: none;
    border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    padding-bottom: 0.25rem;
    margin-bottom: 0.75rem;
    transition: all 0.2s ease;
  }

  @media (hover: hover) {
    .contact-link:hover {
      color: var(--gold-warm);
      border-color: var(--gold-warm);
    }
  }

  .contact-link:focus-visible {
    outline: none;
    box-shadow: var(--focus-ring);
    border-radius: 4px;
  }

  .contact-option-note {
    font-size: 0.875rem;
    color: var(--gray-mid);
    line-height: 1.5;
  }

  .contact-social {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 3rem;
  }

  .contact-social-link {
    font-size: 0.9375rem;
    color: var(--gray-mid);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  @media (hover: hover) {
    .contact-social-link:hover {
      color: var(--gold-base);
    }
  }

  .contact-social-link:focus-visible {
    outline: none;
    box-shadow: var(--focus-ring);
    border-radius: 4px;
  }

  .contact-social-divider {
    color: var(--gray-dark);
  }

  .contact-cta {
    margin-top: 3rem;
  }

  @media (max-width: 768px) {
    .contact {
      padding: 5rem 1.5rem;
    }

    .contact-options {
      grid-template-columns: 1fr;
      gap: 2.5rem;
      margin: 3rem 0 2rem;
    }

    .contact-social {
      flex-wrap: wrap;
    }
  }
</style>

<script>
  // Intersection Observer for fade-in
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    },
    { threshold: 0.1 }
  );

  document.querySelectorAll('.contact .fade-in').forEach(el => {
    observer.observe(el);
  });
</script>
